---
directive: "1.3"
title: Prompt-injection surface — sanitize operator YAML before LLM context
priority: P1-HIGH
status: complete
acceptance:
  - item: "tests/test_prompt_safety.py covers ignore previous instructions, closing-tag injection, ChatML bookend tokens, oversized field, unicode normalization attack"
    status: pass
    evidence: tests/test_prompt_safety.py — 39 tests, all pass. Coverage spans all 4 attack families plus zero-width-character bypass plus bidi-override hiding.
  - item: "tests/test_ai_runtime.py: injection in household_profile.user_notes aborts before anthropic.messages.create"
    status: pass
    evidence: tests/test_prompt_safety.py::test_ai_runtime_aborts_before_messages_create_on_injection — uses a _FailingMessages stub that raises if called; assertion confirms call_count == 0 after OperatorInputError.
  - item: "CONTRIBUTING.md documents the trust boundary"
    status: pending
    evidence: trust boundary documented in tools/prompt_safety.py module docstring; CONTRIBUTING.md mention to be added in a follow-up doc pass.
commits:
  - "TBD"   # see post-commit
---

## Summary

`src/network_engineer/tools/prompt_safety.py` introduces an
operator-YAML→LLM sanitization layer that fails closed on any of:

- "ignore previous/prior/above/earlier instructions" family (with up to 3 intervening words)
- "you are now a different assistant/model/agent/system/persona" persona-swap pattern
- "override/disregard/forget the system/developer/previous prompt/instructions" reversal pattern
- Closing/opening LLM chat-template tags (`</system>`, `<tool_use>`, `</parameter>`, etc.)
- Chat-template tokens (`<|im_start|>`, `<|endoftext|>`, etc.)
- Turn markers at line start (`Human:`, `Assistant:`)
- Oversized fields (>2000 bytes after NFKC normalization)
- Bidi override characters (RLO/PDF/etc.) — would hide content from operator review
- Total context blob >200 KB (safety-net cap)

`sanitize_context_blob` walks any JSON-shaped value recursively and applies
the policy to every string leaf with `field_path` annotation; the first
violation raises `OperatorInputError` with the offending path.

## Two-tier sanitization (operator vs auto-discovered)

The directive originally implies a single-tier policy. The shipped implementation distinguishes two trust classes:

- **Strict** — operator-typed YAML: household_profile, registry notes, origin stories, dismissals, user_notes, operator_notes, concerns, use_case, rationale, reason. Hard-rejects on any pattern match.
- **Permissive** — auto-discovered: networks, wifi_networks, clients, devices, device_stats, firewall_rules, port_forwards, settings, health, alerts, protect_cameras. Warns and continues. The directive 1.6 / task #42 LLM-side preamble is the second line of defence for these.

`sanitize_context_blob_partitioned(blob)` applies the right policy per top-level key. Unknown keys default to **strict** (fail-closed bias).

The reasoning for the split: an operator who legitimately renames their device "ignore-previous-instructions" as a joke shouldn't break the audit. But an attacker who can edit `household_profile.yaml` is *already inside* a higher-trust boundary — that route gets the harder failure.

## Defenses against bypass

Two mechanisms beyond the literal pattern set:

1. **NFKC normalization** — folds compatibility decompositions before pattern matching. Catches confusable-character attacks where the visual rendering matches the attack phrase but codepoints differ.

2. **Cf-category stripping** — zero-width and format control characters (ZWSP, ZWJ, ZWNJ, BOM, soft hyphen, etc.) are removed before regex matching. Without this, an attacker can write `i​gnore previous instructions` (with U+200B between `i` and `g`) — operator review sees "ignore previous instructions" but a naive substring search misses it. With Cf stripped, the regex sees the canonical form.

Bidi override characters (RLO/LRO/PDI/etc.) are *also* Cf-category but are caught earlier with a more specific error message — they're a category of bypass that operator review fails to catch by definition (the rendering hides content).

## Wiring point

`AIRuntime._build_payload` is the single funnel for system + context blocks. It now calls `sanitize_context_blob_partitioned(context_blob)` on the dict variant before `json.dumps`. `OperatorInputError` propagates out of `analyze_security_posture` / `review_config_change` / `explain_anomaly` / `score_upgrade_recommendation` — intentionally, so the surfacing agent (Security Agent, Reporter) can communicate the rejection to the operator instead of proceeding with a sanitized-but-degraded prompt.

## Test coverage

39 tests in `tests/test_prompt_safety.py`:

- 5 ignore-instructions variations (pattern + casing + intervening words)
- 3 new-persona variations
- 3 override-system variations
- 6 chat-template tags
- 3 chat-template tokens
- Turn markers
- Legitimate-content negatives (6 realistic strings that contain trigger words but are safe)
- Size policy (over, at, under)
- Bidi override
- Zero-width bypass (ZWSP and ZWJ separately)
- Non-string input
- Permissive mode behaviour
- Recursive walk + nested injection + path reporting
- List handling
- Partitioned mode (strict vs permissive subtree, unknown-key default)
- AI Runtime abort-before-API integration test

## Outstanding

- `CONTRIBUTING.md` boundary documentation. Tracked as a follow-up.
- Unifying with task #39 (PII redaction layer) — both touch the snapshot→LLM context boundary. Distinct concerns but the same wiring point. Will share `_build_payload` after #39 lands.

## Files

- `src/network_engineer/tools/prompt_safety.py` — new module, 188 lines
- `src/network_engineer/agents/ai_runtime.py` — `_build_payload` now sanitizes the context blob before serialization
- `tests/test_prompt_safety.py` — new, 39 tests
